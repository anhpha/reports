<nav class="navbar navbar-main navbar-fixed-top">
      <div class="container-fluid ">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="{{ path('document_view_category') }}">
              	<span class="glyphicon glyphicon-home menu-icon" aria-hidden="true"></span>
                {{ 'Project management tool'|trans({},'common') }}
          </a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
			{% if app.user and is_granted('ROLE_USER') %}
			<li role="presentation" class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                  	<i class="fa fa-user"></i>
			        {{ 'Profile'|trans({},'common') }} <span class="caret"></span>
                </a>
                <ul class="dropdown-menu">
                  <li><a href="{{ path('apiuser_profile_edit') }}">
                  	 <i class="fa fa-edit"></i>
                      {{ 'Edit'|trans({},'common') }}</a></li>
                  <li><a href="{{ path('fos_user_change_password') }}">
                  	 <i class="fa fa-user-secret"></i>
                      {{ 'Change password'|trans({},'common') }}</a></li>
                </ul>
              </li>
			
			<li><a href="{{ path('fos_user_security_logout') }}">
			    	<i class="fa fa-sign-out"></i>
                      {{ 'Logout'|trans({},'common') }}</a></li>
			{% else %}
            <li><a href="{{ path('apiuser_login') }}">
                <i class="fa fa-sign-in"></i>
			    {{ 'Login'|trans({},'common') }}</a></li>
            {% endif %}
            
{#             <li><a href="#">Help</a></li>#}
          </ul>
          <form class="navbar-form navbar-right" action="{{ path('document_search') }}">
            <input type="text" name="q" class="form-control" placeholder="Document Search...">
          </form>
        </div>
      </div>
</nav>