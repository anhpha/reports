{% extends 'CPSEAPIProjectBundle::layout.html.twig' %}



{% block title %} {{ 'DocumentCategory'|trans({},'documentcategory') }}{% endblock %}

 {% block page_header_title %} {{ 'DocumentCategory'|trans({},'documentcategory') }}{% endblock %}

{% block page_data %}

<table class="table tabl-striped">
    <tbody>				        <tr>
            <th>{{'Parent'|trans({},'documentcategory') }}</th>
            <td>{{ document.parent }}</td>
        </tr>
        		        <tr>
            <th>{{'Name'|trans({},'documentcategory') }}</th>
            <td>{{ document.name }}</td>
        </tr>
        		        <tr>
            <th>{{'Description'|trans({},'documentcategory') }}</th>
            <td>{{ document.description }}</td>
        </tr>
        		        <tr>
            <th>{{'Created_on'|trans({},'documentcategory') }}</th>
            <td>{{ document.createdon|date('Y-m-d H:i:s') }}</td>
        </tr>
        		        <tr>
            <th>{{'Created_by'|trans({},'documentcategory') }}</th>
            <td>{{ document.createdby }}</td>
        </tr>
        		        <tr>
            <th>{{'Updated_on'|trans({},'documentcategory') }}</th>
            <td>{{ document.updatedon|date('Y-m-d H:i:s') }}</td>
        </tr>
        		        <tr>
            <th>{{'Updated_by'|trans({},'documentcategory') }}</th>
            <td>{{ document.updatedby }}</td>
        </tr>
        
    </tbody>
</table>

<div class="col-md-12 actions-list">
    <div class="action">
    	<a class="btn btn-info" href="{{ path('documentcategory_edit', { 'id': document.id }) }}">
            <i class="glyphicon glyphicon-edit" aria-hidden="true"></i>
            {{ 'Edit'|trans({},'common') }}
        </a>
    </div>
    <div class="action">
    	<form class="delete-form" action="{{ path('documentcategory_delete', { 'id': document.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button class="btn btn-danger" type="submit">
            	<i class="glyphicon glyphicon-remove" aria-hidden="true"></i>
                {{ 'Delete'|trans({},'common') }}
            </button>
        </form>
    </div>
</div>
<div class="col-md-12 back-link">
	<a href="{{ path('documentcategory') }}">
            <i class="glyphicon glyphicon-arrow-left" aria-hidden="true"></i>
             {{ 'Back to the list'|trans({},'common') }}
        </a>
</div>{% endblock %}
