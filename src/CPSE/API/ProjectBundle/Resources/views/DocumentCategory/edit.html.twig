{% extends 'CPSEAPIProjectBundle::layout.html.twig' %}



{% block title %} {{ 'DocumentCategory edit'|trans({},'documentcategory') }}{% endblock %}

 {% block page_header_title %} {{ 'DocumentCategory edit'|trans({},'documentcategory') }}{% endblock %}

{% block page_data %}

<div class="col-md-12">
    <div class="col-md-6">

    <form action="{{ path('documentcategory_update', { 'id': document.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_errors(edit_form) }}
                            	            	            	            		<div class="form-group">
                        {% set parent_label %}{{ edit_form.parent.vars.name|capitalize|trans({},'documentcategory') }} {% endset %}
                        {{ form_label(edit_form.parent,parent_label) }}
                        {{ form_widget(edit_form.parent, {'attr':{'class':'form-control'}}) }}
                        {{ form_errors(edit_form.parent) }}
                    </div>
                            	            	            		<div class="form-group">
                        {% set name_label %}{{ edit_form.name.vars.name|capitalize|trans({},'documentcategory') }} {% endset %}
                        {{ form_label(edit_form.name,name_label) }}
                        {{ form_widget(edit_form.name, {'attr':{'class':'form-control'}}) }}
                        {{ form_errors(edit_form.name) }}
                    </div>
                            	            	            		<div class="form-group">
                        {% set description_label %}{{ edit_form.description.vars.name|capitalize|trans({},'documentcategory') }} {% endset %}
                        {{ form_label(edit_form.description,description_label) }}
                        {{ form_widget(edit_form.description, {'attr':{'class':'form-control'}}) }}
                        {{ form_errors(edit_form.description) }}
                    </div>
                            	            	{{ form_rest(edit_form) }}
            <p>
                <button type="submit" class="btn btn-success pull-right">
                	<i class="glyphicon glyphicon-edit" aria-hidden="true"></i>        
                     {{ 'Update'|trans({},'common') }}
                </button>
            </p>
    </form>
</div>
</div>


<div class="col-md-12 actions-list">
</div>
<div class="col-md-12 back-link">
	<a href="{{ path('documentcategory') }}">
            <i class="glyphicon glyphicon-arrow-left" aria-hidden="true"></i>
             {{ 'Back to the list'|trans({},'common') }}
        </a>
</div>{% endblock %}